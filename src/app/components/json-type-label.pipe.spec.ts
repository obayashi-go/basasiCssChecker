import { MatchCaseLabel } from './match-case-label';

describe('JsonTypeLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new MatchCaseLabel();
    expect(pipe).toBeTruthy();
  });
});
